#Create a new table named car with the fields make, model, color, vin, and release_year
CREATE TABLE car (
    vin BIGINT,
    make VARCHAR(255),
    model VARCHAR(255),
    color VARCHAR(255),
    release_year YEAR
);
